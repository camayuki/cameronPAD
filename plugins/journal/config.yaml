name: "journal"
version: "1.0.0"
enabled: true
description: "Daily journal with calendar view and image uploads"
settings:
  upload_dir: "data/uploads/journal"  # Directory for uploaded images
  max_image_size: 10485760  # 10MB max per image
